export function toast(message){const root=document.querySelector('#toastRoot')||document.body;const el=document.createElement('div');el.className='toast';el.textContent=message;root.appendChild(el);setTimeout(()=>el.remove(),2800)}
export function setupTheme(){const saved=localStorage.getItem('sky7-theme')||'dark';document.documentElement.dataset.theme=saved;document.querySelectorAll('[data-theme-toggle]').forEach(b=>b.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem('sky7-theme',next)}))}
setupTheme();document.querySelectorAll('[data-toggle-password]').forEach(b=>b.addEventListener('click',()=>{const i=document.getElementById(b.dataset.togglePassword);i.type=i.type==='password'?'text':'password'}));document.querySelectorAll('[data-toast]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();toast(a.dataset.toast)}));
